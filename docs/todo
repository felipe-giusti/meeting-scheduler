
### WORKING ON
# PDF READER
 - add logic when more than 1 page
   - participant names / emails for every page, rest of the information only first page
     - * check if company name field can change later
 - add date parser logic to model


### Main functionality

# Meeting Scheduler
- base class for a scheduler
  - create meeting
  - edit meeting
  - Send notification via scheduler?
- impl for Zoom
- impl for Teams
    refs:
    https://learn.microsoft.com/en-us/graph/api/application-post-onlinemeetings?view=graph-rest-1.0&tabs=http
    https://learn.microsoft.com/en-us/graph/api/resources/onlinemeeting?view=graph-rest-1.0

# Custom email sender
    - msg
    - to
    - cc
    - attachments

# interface
 - add interface to show document with annotations
 - add interface to customize annotations -- select area ???
 - validation step -- show errors / ... - ex: len(emails) != len(names)

